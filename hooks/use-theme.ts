/**
 * Theme Hook
 *
 * Re-export of next-themes useTheme hook for centralized imports.
 * Use this instead of importing directly from next-themes.
 *
 * @example
 * ```tsx
 * import { useTheme } from "@/hooks/use-theme";
 *
 * const { theme, setTheme } = useTheme();
 * ```
 */
export { useTheme } from "next-themes";
